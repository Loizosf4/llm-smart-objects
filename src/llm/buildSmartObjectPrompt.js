import { buildSmartObjectResponseSchema } from "../validation/smartObjectSchema.js";

function formatNeeds(needs) {
  return needs.map((need) => [
    `Need: ${need.name}`,
    `Definition: ${need.definition}`,
    "Weak calibration reference:",
    `- interaction: ${need.weakReference.example}`,
    `- weight: ${need.weakReference.weight}`,
    "Strong calibration reference:",
    `- interaction: ${need.strongReference.example}`,
    `- weight: ${need.strongReference.weight}`
  ].join("\n")).join("\n\n");
}

export function buildSmartObjectPrompt({ locationDescription, needs }) {
  const responseSchema = buildSmartObjectResponseSchema(needs);

  return [
    "You generate JSON for an experimental LLM Smart Object Generator.",
    "",
    "Task:",
    "Generate a concise set of useful physical or environmental objects appropriate for the described location.",
    "For each object, generate one or more meaningful interactions. Each interaction may advertise only the supplied NPC needs it can meaningfully satisfy.",
    "",
    `Location description: ${locationDescription}`,
    "",
    "Allowed needs, definitions, and calibration references:",
    formatNeeds(needs),
    "",
    "Meaning of an interaction and advertisement:",
    "An object is a physical or environmental entity.",
    "An interaction describes one complete way an NPC can use that object, such as sit_and_relax, sleep, watch_television, read_a_book, get_and_drink_coffee, or get_and_eat_food.",
    "Prefer complete need-satisfying interactions over incomplete enabling steps. For example, use get_and_drink_coffee instead of press_button, and read_a_book instead of take_book.",
    "An advertisement means the interaction communicates that performing it can help satisfy a particular NPC need.",
    "The advertisement weight is the base strength with which performing that interaction satisfies the advertised need.",
    "",
    "Duration model:",
    "Every interaction requires exactly one duration object.",
    "Allowed duration types are instant, fixed, and continuous.",
    "Use instant when the interaction completes almost immediately and does not meaningfully occupy the NPC over simulation time. Instant durations must omit seconds.",
    "Use fixed when the interaction has a natural completion point and takes an approximately predictable amount of time. Fixed durations require plausible real-world seconds greater than 0 and no greater than 86400.",
    "Use continuous when there is no predetermined completion time and a future NPC or utility system may decide when to stop. Continuous durations must omit seconds.",
    "Duration applies to the complete interaction, not one animation or small internal step.",
    "Do not classify every interaction as fixed. Do not classify every interaction as continuous. Use instant only when time is negligible for decision-making.",
    "Duration examples:",
    "- sit_and_relax -> continuous",
    "- sleep -> continuous",
    "- watch_television -> continuous",
    "- get_and_drink_coffee -> fixed, approximately 30-120 seconds",
    "- eat_a_meal -> fixed, approximately 600-1800 seconds",
    "- wash_hands -> fixed, approximately 20-60 seconds",
    "- turn_on_light -> instant",
    "",
    "Availability model:",
    "Every interaction requires exactly one availability object.",
    "Availability describes whether this interaction should currently be visible as a possible choice from the object side.",
    "Availability belongs to the interaction. Different interactions on the same object may have different availability types if that genuinely makes sense.",
    "The only allowed availability types are always and when_free.",
    "Use when_free when the interaction should be exclusive to one NPC in this simplified version.",
    "Use always when the interaction can remain advertised while another NPC is using or observing the object.",
    "Use always for naturally shared objects in this version, because capacity is not supported yet.",
    "Do not classify everything as when_free. Do not classify everything as always.",
    "Do not generate runtime occupancy data such as is_free, occupied, current_users, reserved_by, or user_count.",
    "Do not generate capacity, seat counts, user limits, available slot counts, reservations, queues, availability expressions, or arbitrary Boolean logic.",
    "Availability examples:",
    "- single chair -> sit_and_rest -> when_free",
    "- single bed -> sleep -> when_free",
    "- shower -> take_shower -> when_free",
    "- toilet -> use_toilet -> when_free",
    "- vending machine -> get_and_consume_snack -> when_free",
    "- television -> watch_television -> always",
    "- radio -> listen_to_radio -> always",
    "- notice board -> read_information -> always",
    "- large shared table -> socialize -> always",
    "",
    "Need-specific weight calibration:",
    "Calibration references define the approximate low and high ends for each specific need.",
    "Compare each newly generated interaction against both references for that need and assign a proportional value based on relative strength.",
    "Do not simply copy the weak or strong reference value. Values between the references are expected.",
    "A generated interaction may reasonably be weaker than the weak reference or stronger than the strong reference only in exceptional cases.",
    "Never exceed 1.0.",
    "Do not generate a reference object merely because it appears in the calibration context.",
    "The references are examples, not mandatory objects for the location.",
    "Per-need calibration references are more important than the generic global weight bands.",
    "Generic fallback scale:",
    "- 0.01-0.19 = minimal effect",
    "- 0.20-0.39 = weak effect",
    "- 0.40-0.59 = moderate effect",
    "- 0.60-0.79 = strong effect",
    "- 0.80-1.00 = very strong effect",
    "Omit advertisements for needs the interaction does not reasonably help. Do not normally generate 0.0.",
    "",
    "Generation rules:",
    "1. Generate appropriate physical or environmental objects for the location.",
    "2. Generate one or more meaningful interactions for each object.",
    "3. Prefer one interaction when one is enough.",
    "4. Add multiple interactions only when they represent genuinely different uses.",
    "5. Use complete need-satisfying interactions, not partial enabling steps.",
    "6. Attach advertisements to interactions, not directly to objects.",
    "7. Add a duration object to every interaction.",
    "8. Add an availability object to every interaction.",
    "9. Use only availability.type values always or when_free.",
    "10. Use only the exact need names supplied above.",
    "11. Assign weights according to how strongly the interaction satisfies the need.",
    "12. Consider relative strengths between interactions.",
    "13. Avoid assigning every weight the same value.",
    "14. Avoid assigning nearly every value close to 1.0.",
    "15. Do not generate advertisements with no reasonable relationship to the interaction.",
    "16. Generate unique stable object IDs such as sofa_01.",
    "17. Generate stable interaction IDs such as sit_and_relax using lowercase letters, numbers, and underscores.",
    "18. Return only valid raw JSON.",
    "19. Follow the exact schema.",
    "20. Do not return any fields outside the schema.",
    "21. Do not generate capacity, seat count, maximum users, occupancy count, current users, occupant IDs, claimed slots, reservations, reservation timeouts, queues, queue positions, resources, stock, object state, powered state, locked state, operational state, preconditions, arbitrary conditions, NPC need conditions, NPC role conditions, schedules, animation, execution templates, Behavior Trees, action sequences, behavior logic, utility formulas, utility curves, arbitrary code, or simulator logic.",
    "",
    "Required JSON schema:",
    JSON.stringify(responseSchema, null, 2),
    "",
    "Return raw JSON only. Do not include Markdown, code fences, comments, or explanations."
  ].join("\n");
}

export function buildSmartObjectRepairPrompt({ invalidOutput, validationErrors, locationDescription, needs }) {
  const responseSchema = buildSmartObjectResponseSchema(needs);

  return [
    "Correct the invalid smart-object JSON output.",
    "Return corrected raw JSON only. Do not include Markdown, code fences, comments, or explanations.",
    "Objects must contain interactions, and interactions must contain advertisements. Do not put advertisements directly on objects.",
    "Every interaction must contain a duration object.",
    "Allowed duration types are instant, fixed, and continuous.",
    "Fixed durations require plausible real-world seconds greater than 0 and no greater than 86400.",
    "Instant and continuous durations must omit seconds.",
    "Use continuous when there is no predetermined stopping time. Use fixed when the interaction naturally completes. Use instant only when time is negligible.",
    "Every interaction must contain an availability object.",
    "Availability belongs to the interaction and must be an object with only a type field.",
    "Allowed availability types are always and when_free.",
    "Use when_free when the interaction should be exclusive to one NPC.",
    "Use always when the interaction can remain advertised while another NPC is using or observing the object.",
    "Do not classify everything as when_free. Do not classify everything as always.",
    "Guidance examples: single chair -> sit_and_rest -> when_free; single bed -> sleep -> when_free; shower -> take_shower -> when_free; toilet -> use_toilet -> when_free; vending machine -> get_and_consume_snack -> when_free; television -> watch_television -> always; radio -> listen_to_radio -> always; notice board -> read_information -> always; large shared table -> socialize -> always.",
    "Use complete need-satisfying interactions, not partial enabling steps.",
    "Do not generate runtime occupancy data, capacity, seat counts, user limits, available slot counts, reservations, queues, availability expressions, arbitrary Boolean logic, resources, stock, object state, powered state, locked state, operational state, preconditions, animation references, execution templates, Behavior Trees, action sequences, behavior logic, NPC stop conditions, NPC roles, NPC personalities, utility formulas, utility curves, arbitrary code, or simulator logic.",
    "",
    `Location description: ${locationDescription}`,
    "",
    "Allowed needs, definitions, and calibration references:",
    formatNeeds(needs),
    "",
    "Use the weak and strong calibration references to repair advertisement weights. The references are examples only; do not automatically generate reference objects. Per-need calibration takes priority over the generic scale: 0.01-0.19 minimal, 0.20-0.39 weak, 0.40-0.59 moderate, 0.60-0.79 strong, 0.80-1.00 very strong.",
    "",
    "Validation errors:",
    validationErrors.map((error) => `- ${error}`).join("\n"),
    "",
    "Required JSON schema:",
    JSON.stringify(responseSchema, null, 2),
    "",
    "Invalid output to repair:",
    invalidOutput
  ].join("\n");
}
